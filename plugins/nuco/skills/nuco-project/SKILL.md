---
name: nuco-project
description: Switch project, or list projects. Runs on /nuco-project [name].
argument-hint: "[name]"
---
The **project** view. No argument → list the projects (root). A name → move the cursor into that project and render its **project home**.

Layout is owned entirely by the **`nuco`** skill's Rendering spec — apply it (read the `nuco` skill if it isn't already loaded). Don't invent a layout here.
