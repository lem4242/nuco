---
name: nuco-search
description: Search the nuco store — docs and files. Runs on /nuco-search <query>.
argument-hint: "<query>"
---
The **search** view. Search docs (`doc_search`) and files (`file_search`) in the current project — or across everything if no cursor is set.

Layout is owned entirely by the **`nuco`** skill's Rendering spec — apply it (read the `nuco` skill if it isn't already loaded). Don't invent a layout here.
